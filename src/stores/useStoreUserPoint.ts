// import { create } from 'zustand'

// import { getUserPoint } from '@/api/vault'

// export const useStoreUserPoint = create((set) => ({
//   data: {},
//   update: async (uid: string, vid: string) => {
//     const data = await getUserPoint(uid, vid)
//     set()
//   },
// }))
